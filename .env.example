# PIN do wejścia (6 cyfr). Ustaw przynajmniej jedną z tych zmiennych.
# VITE_SITE_PIN – działa od razu w dev (Vite ładuje to z .env).
# SITE_PIN – wstrzykiwany przez vite.config (też działa w dev po restarcie).

SITE_PIN=111111
